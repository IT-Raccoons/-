#include <iostream>
#include <string>
using namespace std;
int main() {
	
	
	/* Переменные */
	/* Счетчик */
	long long i, weight[3];
	/* Строки */ 
	string money[3];
	
	cin >> money[0] >> money[1] >> money[2];
	
	for (i=0; i<=2; i++) {
		weight[i] = money[i].size();
	}
	cout << weight[0] << endl;
	cout << weight[1] << endl;
	cout << weight[2] << endl;
	
	#include <iostream>
#include <string.h>

using namespace std;
int main()
{
	string a,b,max;
	cin>>a>>b;
	for (int i=0;i<a.size();i++)
	{
      if ((int)a.at(i)>(int)b.at(i))
       {
      max=a;
      break;
   	}
      else if ((int)a.at(i)<(int)b.at(i))
      {
      	max=b;
      	break;
      }
	}
	cout<<max;""
}
A 
	/*
	for (weight[i]=""; weight<money[i].size(); i++) {
		weight[i] += 1;
	}
	cout << weight[0] << endl;
	*/
	return 0;
}
