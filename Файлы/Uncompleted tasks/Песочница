#include <iostream>
#include <string>
using namespace std;
int main() {
	int n, m;
	cin >> n >> m;
	 string k[n];
	for (int i = 1; i <= 3*n; i++) {
		getline(cin, k[i]);
	}
	string p[m];
	for (int i = 1; i <= m; i++) {
		cin >> p[i];
	}
	for (int i = 1; i <= m; i++) {
		for (int j = 1; j <= 3*n; j++) {
			if (k[j] == p[i]) {
				cout << k[j] << " (" << k[j+2]<< ")" << endl;
			}
		}
	}
	return 0;
}
